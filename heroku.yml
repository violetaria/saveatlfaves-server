build:
  docker:
    web: ./Dockerfile
run:
  web: gunicorn --bind 0.0.0.0:$PORT -c gunicorn.py carebackend.wsgi
release:
  image: web
  command:
    - python3 manage.py migrate --settings carebackend.settings.base
